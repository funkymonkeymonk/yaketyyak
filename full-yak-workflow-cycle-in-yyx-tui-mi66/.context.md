# Goal
Close the gap between model_test.go (the test spec) and the current TUI implementation, enabling create/manage/watch of the full yak workflow lifecycle from inside yyx.

# Sub-yaks (implement in this order)
1. go-ts schema contract — no dependencies, do first
2. temporal type foundations — unblocks everything else
3. activity timeline spans — can parallel with shave dispatch
4. shave workflow dispatch from tui — requires temporal type foundations
5. temporal live polling in tui — requires shave dispatch
6. workflow history viewer — requires temporal foundations + activity spans
7. sidebar shave workflow status — requires shave dispatch (for Repo.ShaveState)

# Acceptance Criteria
- [ ] All tests in tui/model_test.go pass: `devenv tasks run yyx:test`
- [ ] Binary builds: `devenv tasks run yyx:build`
- [ ] No regressions in existing passing tests
