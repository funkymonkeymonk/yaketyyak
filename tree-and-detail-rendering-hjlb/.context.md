Render yak tree with Unicode box-drawing (├─ ╰─ │) and state-based colors (todo=white ○, wip=yellow ●, done=dim ✓). Detail pane shows metadata, context preview, workflow phase, signal actions. Status bar with summary and key hints. Uses lipgloss for styling.