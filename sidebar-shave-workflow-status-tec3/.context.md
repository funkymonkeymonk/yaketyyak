# Goal
Show live shave workflow status in the yak sidebar, including phase, iteration count, and workspace. Show "not started" for repos with no workflow.

# Files
- `tui/sidebar.go` — update ShowYak() signature and implementation; update ShowRepo()

# Changes

`ShowYak(yak *YakLine, repo *Repo, width int)` gains the `repo *Repo` parameter.
Call site in `tui/model.go` updateSidebar() updated to pass `&m.repos[tl.repoIdx]`.

When `repo.ShaveState != nil && repo.ShaveState.YakName == yak.Name`:
- Append a "Shave Workflow" section after the existing content:
  ```
  ── Shave Workflow ──────────────
  Phase:      implementing
  Iteration:  1/3
  Workspace:  shave-testyak
  ```
- Renders for ALL phases including "failed" and "done"

`ShowRepo()` — when repo has no WFID (empty string):
- Add "Workflow: not started" line in the repo detail

# Acceptance Criteria
- [ ] TestSidebarShowsShaveWorkflowStatus passes (shows "Shave Workflow", "implementing", "1/3")
- [ ] TestSidebarShowsFailedShaveWorkflow passes (shows "Shave Workflow", "failed")
- [ ] TestSidebarShowsDoneShaveWorkflow passes (shows "Shave Workflow", "done")
- [ ] TestSidebarEmptyState passes (shows "not started" for empty repo)
- [ ] TestSidebarShowsContent passes (still shows "Actions" section)

# Dependencies
Requires: temporal type foundations (for ShaveState type), shave workflow dispatch from tui (for Repo.ShaveState field)
