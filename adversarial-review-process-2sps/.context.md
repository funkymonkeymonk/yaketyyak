When a coding agent reports done, a fresh independent agent verifies the work against the original brief — without knowing the implementer's reasoning. Prevents anchoring bias. Yakthang uses subagents (not yak-box spawn) for review since reviewers are read-only. Should define: trigger condition, reviewer prompt template, how verdict transitions wip-state.