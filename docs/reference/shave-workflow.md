> **This feature is not yet implemented.** This document describes planned behaviour.

# ShaveWorkflow Reference

A standalone Temporal workflow that shaves a single yak through the shave loop pattern: implement → validate → adversarial review → create PR → merge.

This workflow does not exist in the current codebase. The implemented workflow is `YakWorkflow` — see [YakWorkflow Reference](yak-workflow.md).

> For the real workflow, see [YakWorkflow Reference](yak-workflow.md).
> For the design rationale behind this planned workflow, see [Barber and Shave Workflows](../explanation/barber-and-shave-workflows.md).
