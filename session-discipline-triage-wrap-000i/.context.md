Pre-commitment ritual for every work session: triage (hard stop time, WIP limit, scope survey) → work → wrap (harvest done work into worklog, prune map, reorganize remaining). Prevents unbounded agentic loops. Yakthang uses this as a non-negotiable pattern. Should define: when/how triage triggers, how session params are stored in yx, wrap automation.