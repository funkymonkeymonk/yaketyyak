Create a Justfile with standardized commands: just build, just test, just lint, just install, just clean. Standardizes entry points for both humans and agents. Currently build commands are scattered across devenv config and README.