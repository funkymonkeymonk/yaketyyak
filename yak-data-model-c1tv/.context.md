YakLine struct and LoadYaks function that walks .yaks/ directory on filesystem, reads .state/.name/.id/.context.md, computes tree metadata (depth, sibling position, ancestor continuations). No external yx CLI dependency.