A long-lived orchestrator agent (like Yakob) that plans work, writes task context, spawns coding agents, monitors progress, and reacts to blockers — without implementing directly. The Temporal workflow handles durability; the agent handles task-level decisions.