A long-lived orchestrator agent (like Yakob) that plans work, writes task context, spawns coding agents, monitors progress via yx ls, and reacts to blockers — without implementing directly. The Temporal workflow handles durability/crash-recovery at the infrastructure layer; the orchestrator agent handles task-level decisions. Should define: agent prompt, interaction protocol with Temporal workflow, how it reads yx state and decides next actions.