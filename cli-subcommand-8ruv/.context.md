Cobra subcommand `yyx tui` with flags: --repo owner/repo, --repo-root /path, --agent pi|claude-code|codex|opencode, --yaks-dir .yaks. Registers in cmd/tui.go. Starts Bubble Tea program with tea.WithAltScreen(). Defaults to .yaks/ directory. Temporal features gated on --repo and --repo-root being provided.