# Problem
The CLI binary is named `yyx` but should be renamed to `yy` for brevity.

# Acceptance Criteria
- [ ] Binary is built as `yy` (not `yyx`)
- [ ] All references to `yyx` in cmd/ and main.go use/reflect the new name
- [ ] `devenv` tasks that reference the binary name are updated
- [ ] README and docs updated to use `yy`

# Files
- main.go (entry point, may reference binary name)
- cmd/root.go (Cobra root command Use/Short/Long fields)
- devenv.nix (binary name in package/task definitions)
- docs/ (any references to `yyx`)

# Notes
- The Cobra root command's `Use` field should be updated to `yy`
- Check for hardcoded `yyx` strings across the repo
